## Downloading, configuring, and installing QT.


#### + Using the apt Package Manager.

```
sudo apt install qtk-base-dev
```


#### + Downloading Qt from GitHub.

````
git clone git://code.qt.io/qt/qt5.git
````